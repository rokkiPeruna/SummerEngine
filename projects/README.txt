To create new project, copy 'example_project' folder,
name it the way you want and change the CURRENT_PROJECT_NAME in Engine's main.cpp
to match the folder name.

To deploy game, copy .exe and all .dlls and .libs from 'executables' folder under
desired build (Debug, Release, MinSizeRel, RelWithDebInfo) to your game's folder.